{
  "name": "staticExpressApp",
  "version": "0.0.0",
  "description": "A simple Express app that serves static HTML, CSS, and JS files",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Davidson",
  "license": "ISC",
  "dependencies": {
    "express": "^4.8.0"
  }
}
